<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cheatgz.lostandfoundsystem">

    <!--允许程序打开网络套接字-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--允许程序设置内置sd卡的写权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许程序获取网络状态-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--允许程序访问WiFi网络信息-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--允许程序读写手机状态和身份-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!--允许程序访问CellID或WiFi热点来获取粗略的位置-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="c97601d51f7c54ec3a35f1f2c2d4e67d"/>

        <activity
            android:name=".sign_in">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".main"></activity>
        <activity android:name=".sign_up"></activity>
        <activity android:name=".set"></activity>
        <activity android:name=".info"></activity>
        <activity android:name=".lost1"></activity>
        <activity android:name=".lost2"></activity>
        <activity android:name=".found1"></activity>
        <activity android:name=".found2"></activity>
        <activity android:name=".message"></activity>
        <activity android:name=".property"></activity>
        <activity android:name=".authentication"></activity>
        <activity android:name=".wallet"></activity>

    </application>

</manifest>
